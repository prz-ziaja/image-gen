from setuptools import find_packages, setup

setup(name='image_gen',
      version='0.1',
      description='ML package image generation.',
      package_dir={"": "src"},
      packages=find_packages(where="src"),
      url="https://github.com/prz-ziaja/image-gen",
          author="prz-ziaja",
    author_email="None",
     )
